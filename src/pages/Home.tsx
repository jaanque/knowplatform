import Hero from '../components/Hero';
import FeaturedFiles from '../components/FeaturedFiles';

const Home = () => {
  return (
    <main>
      <Hero />
      <FeaturedFiles />
    </main>
  );
};

export default Home;
